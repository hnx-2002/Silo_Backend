// This file is generated by Table2API.
// If you met a bug, please tell me, thanks!
// wrx 20220126

using System;
using System.Collections.Generic;

namespace T2ACore;

/// <summary>
/// 通用方法
/// </summary>
public static class FunConsole
{
    /// <summary>
    /// Console日志
    /// </summary>
    /// <param name="pattern">标题</param>
    /// <param name="para1">打印值1</param>
    /// <param name="para2">打印值2</param>
    /// <param name="para3">打印值3</param>
    public static void ConsoleLog(string pattern, string para1 = "", string para2 = "", string para3 = "")
    {
        var dt = DateTime.Now.ToString(" yyyy-MM-dd HH:mm:ss.fff ");
        Console.WriteLine("--" + pattern + "--开始" + dt + "-------");
        if (para1 != "") Console.WriteLine(para1);
        if (para2 != "") Console.WriteLine(para2);
        if (para3 != "") Console.WriteLine(para3);
        Console.WriteLine("--" + pattern + "--结束" + dt + "-------");
    }

    /// <summary>
    /// Console输出
    /// </summary>
    /// <param name="msg"></param>
    public static void Debug(string msg)
    {
        Console.WriteLine(msg);
    }

    /// <summary>
    /// Console输出
    /// </summary>
    /// <param name="msg"></param>
    public static void Log(string msg)
    {
        Console.WriteLine(msg);
    }

    /// <summary>
    /// Console输出,带颜色
    /// </summary>
    /// <param name="msg"></param>
    /// <param name="color"></param>
    public static void Log(string msg, ConsoleColor color)
    {
        var tempColor = Console.ForegroundColor;
        Console.ForegroundColor = color;
        Console.WriteLine(msg);
        Console.ForegroundColor = tempColor;
    }

    /// <summary>
    /// Console输出,带颜色
    /// </summary>
    /// <param name="msgs"></param>
    /// <param name="color"></param>
    public static void Log(List<string> msgs, ConsoleColor color)
    {
        var tempColor = Console.ForegroundColor;
        Console.ForegroundColor = color;
        foreach (var msg in msgs)
        {
            Console.WriteLine(msg);
        }
        Console.ForegroundColor = tempColor;
    }
}
<script setup>
//This file is generated by Table2API.
//If you met a bug, please tell me, thanks!
//wrx 20241009
import { onMounted, ref, computed, useTemplateRef } from 'vue';
import { NSpace, NDataTable, NButton, NFormItem, NInput, NPagination, NSelect } from 'naive-ui';
import {
    make_text_NButton,
    make_h_NSpace,
    h_tag,
    make_download_Abutton,
} from '@utility/NaiveHComponent';
import { confirmDel } from '@common/message/confirmDel';
import moment from 'moment';
import Layout_UMD from '@common/layoutIndi/Layout_UMD.vue';
import { GetOne, Delete, MultiPagedSearch, GetOptions } from '@request/Basic_Request.js';
import { findLabel } from '@utility/OptionsTool.js';
import { executeRes } from '@utility/resultExecute.js';
import { translate } from '@utility/i18n.js';
import task_result_Form from './task_result_Form.vue';
import {
    get_task_result_layout_type_options,
} from './options_task_result.js';

const itemName = 'Task_result';

//检索条件
const queryInfo = ref({
    page: 1,
    pageSize: 15,
    task_base_id: '',
    layout_title: '',
    rfa_resource_id: '',
    create_account: '',
    update_account: '',
});

const tableLoading = ref(false); //加载中
const columns = ref([]); //显示的列
const tableData = ref([]); //表格数据
const totalCount = ref(0); //数据总数

const task_result_FormRef = useTemplateRef('task_result_FormRef'); //表单控件

const emit = defineEmits([]);
const inPara = defineProps({});

onMounted(() => {
    //注意，加载选项时可能需要异步处理
    //TODO,整理成异步形式
    refresh_task_result_layout_type_options(() => {});
    refresh_task01_base_options(() => {});
    resetColumns();
    initData();
});

//初始化数据
function initData() {
    tableLoading.value = true;
    if (!queryInfo.value.task_base_id) {
        queryInfo.value.task_base_id = '00000000-0000-0000-0000-000000000000';
    }
    if (!queryInfo.value.rfa_resource_id) {
        queryInfo.value.rfa_resource_id = '00000000-0000-0000-0000-000000000000';
    }
    MultiPagedSearch(queryInfo.value, itemName)
        .then((res) => {
            tableData.value = res.datas;
            totalCount.value = res.totalCount;
            tableLoading.value = false;
            queryInfo.value.task_base_id = '';
            queryInfo.value.rfa_resource_id = '';
        })
        .catch((err) => console.error(err));
}

//重置列
function resetColumns() {
    columns.value = [
        //{
        //    title: computed(() => translate.t('序号')),
        //    align: 'center',
        //    width: '50px',
        //    render(row, index) {
        //        return index + 1;
        //    },
        //},
        //        {
        //            title: computed(() => translate.t('Id')),
        //            key: 'id',
        //            width: '100px',
        //            ellipsis: {
        //                tooltip: true,
        //            },
        //            sorter: 'default',
        //        },
        //        {
        //            title: computed(() => translate.t('任务Id')),
        //            key: 'task_base_id',
        //            width: '100px',
        //            ellipsis: {
        //                tooltip: true,
        //            },
        //            sorter: 'default',
        //            render(val, idx) {
        //                return findLabel(task01_base_options.value, val.task_base_id);
        //            },
        //        },
        {
            title: computed(() => translate.t('排序')),
            key: 'sort',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title: computed(() => translate.t('布置标题')),
            key: 'layout_title',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        //        {
        //            title: computed(() => translate.t('族id')),
        //            key: 'rfa_resource_id',
        //            width: '100px',
        //            ellipsis: {
        //                tooltip: true,
        //            },
        //            sorter: 'default',
        //        },
        {
            title: computed(() => translate.t('布置类型')),
            key: 'layout_type',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
            render(val, idx) {
                return findLabel(task_result_layout_type_options.value, val.layout_type);
            },
        },
        {
            title: computed(() => translate.t('布置x坐标')),
            key: 'location_x',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title: computed(() => translate.t('布置y坐标')),
            key: 'location_y',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title: computed(() => translate.t('布置z坐标')),
            key: 'location_z',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title: computed(() => translate.t('旋转轴x方向')),
            key: 'normal_x',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title: computed(() => translate.t('旋转轴y方向')),
            key: 'normal_y',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title: computed(() => translate.t('旋转轴z方向')),
            key: 'normal_z',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title: computed(() => translate.t('旋转角度')),
            key: 'rotate_angle',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        //        {
        //            title: computed(() => translate.t('创建账号')),
        //            key: 'create_account',
        //            width: '100px',
        //            ellipsis: {
        //                tooltip: true,
        //            },
        //            sorter: 'default',
        //        },
        {
            title: computed(() => translate.t('创建人')),
            key: 'create_username',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title: computed(() => translate.t('创建时间')),
            key: 'create_time',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
            render(val, idx) {
                return moment(val.create_time).utc(true).format('YYYY-MM-DD HH:mm:ss.SSS');
            },
        },
        //        {
        //            title: computed(() => translate.t('更新账号')),
        //            key: 'update_account',
        //            width: '100px',
        //            ellipsis: {
        //                tooltip: true,
        //            },
        //            sorter: 'default',
        //        },
        {
            title: computed(() => translate.t('更新人')),
            key: 'update_username',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title: computed(() => translate.t('更新时间')),
            key: 'update_time',
            width: '100px',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
            render(val, idx) {
                return moment(val.update_time).utc(true).format('YYYY-MM-DD HH:mm:ss.SSS');
            },
        },
        //        {
        //            title: computed(() => translate.t('备注')),
        //            key: 'remark',
        //            width: '100px',
        //            ellipsis: {
        //                tooltip: true,
        //            },
        //            sorter: 'default',
        //        },
        {
            title: computed(() => translate.t('操作')),
            width: 160,
            render(row) {
                let opts = [];
                opts.push(make_text_NButton(translate.t('编辑'), () => editRow(row)));
                opts.push(
                    make_text_NButton(translate.t('删除'), () =>
                        confirmDel(translate, () => delRow(row))
                    )
                );
                //opts.push(make_text_NButton(translate.t('选中'), () => pickRow(row)));
                return make_h_NSpace(() => opts);
            },
        },
    ];
}

//高级查询
function queryList() {
    queryInfo.value.page = 1;
    queryInfo.value.pageSize = 15;
    initData();
}
//重置查询
function resetQuery() {
    queryInfo.value.page = 1;
    queryInfo.value.pageSize = 15;
    queryInfo.value.task_base_id = '';
    queryInfo.value.layout_title = '';
    queryInfo.value.rfa_resource_id = '';
    queryInfo.value.create_account = '';
    queryInfo.value.update_account = '';
    initData();
}

//新任务
function newTask() {
    task_result_FormRef.value.open('add', {}, [task01_base_options.value]);
}

//编辑
function editRow(row) {
    GetOne(row.id, itemName).then((res) => {
        task_result_FormRef.value.open('edit', res, [task01_base_options.value]);
    });
}

//删除
function delRow(row) {
    Delete([row.id], itemName).then((res) => {
        executeRes(res, translate, () => {
            initData();
        });
    });
}

//翻页
function pageChange(page) {
    queryInfo.value.page = page;
    initData();
}
//改变页码
function pageSizeChange(pageSize) {
    queryInfo.value.page = 1;
    queryInfo.value.pageSize = pageSize;
    initData();
}

////选中行
//function pickRow(row) {
//    console.log(row.id);
//}

//task_result_layout_type选项
const task_result_layout_type_options = ref([]);
//刷新task_result_layout_type选项
function refresh_task_result_layout_type_options() {
    task_result_layout_type_options.value = get_task_result_layout_type_options(translate);
}

//task01_base选项
const task01_base_options = ref([]);
function refresh_task01_base_options(cb) {
    GetOptions('Task01_base').then((res) => {
        task01_base_options.value = res;
        cb();
    });
}
</script>

<template>
    <Layout_UMD>
        <template #header>
            <NSpace justify="space-between">
                <!-- 高级查询 -->
                <NSpace>
                    <NFormItem :label="translate.t('任务Id')" label-placement="left">
                        <NSelect
                            filterable
                            style="width: 200px"
                            clearable
                            v-model:value="queryInfo.task_base_id"
                            :options="task01_base_options"
                        ></NSelect>
                    </NFormItem>
                    <NFormItem :label="translate.t('布置标题')" label-placement="left">
                        <NInput v-model:value="queryInfo.layout_title" />
                    </NFormItem>
                    <!-- <NFormItem :label="translate.t('族id')" label-placement="left">
                        <NInput v-model:value="queryInfo.rfa_resource_id" />
                    </NFormItem> -->
                    <!-- <NFormItem :label="translate.t('创建账号')" label-placement="left">
                        <NInput v-model:value="queryInfo.create_account" />
                    </NFormItem> -->
                    <!-- <NFormItem :label="translate.t('更新账号')" label-placement="left">
                        <NInput v-model:value="queryInfo.update_account" />
                    </NFormItem> -->
                    <NButton type="primary" @click="queryList">{{ translate.t('查询') }}</NButton>
                    <NButton @click="resetQuery">{{ translate.t('重置') }}</NButton>
                </NSpace>
                <NSpace>
                    <NButton type="primary" @click="newTask">
                        {{ translate.t('添加') }}
                    </NButton>
                </NSpace>
            </NSpace>
        </template>
        <template #list>
            <NDataTable
                ref="table"
                bordered
                striped
                :loading="tableLoading"
                :columns="columns"
                :data="tableData"
                :style="{ height: `100%` }"
                flex-height
            ></NDataTable>
        </template>
        <template #footer>
            <NSpace>
                <NPagination
                    v-model:page="queryInfo.page"
                    v-model:page-size="queryInfo.pageSize"
                    :item-count="totalCount"
                    show-size-picker
                    :page-sizes="[15, 30, 45]"
                    :on-update:page="pageChange"
                    :on-update:page-size="pageSizeChange"
                    :prefix="
                        ({ itemCount }) => {
                            return translate.t('共') + ' ' + itemCount + ' ' + translate.t('条');
                        }
                    "
                />
            </NSpace>
        </template>
    </Layout_UMD>
    <task_result_Form ref="task_result_FormRef" @sheetDone="initData()"></task_result_Form>
</template>
<style scoped></style>

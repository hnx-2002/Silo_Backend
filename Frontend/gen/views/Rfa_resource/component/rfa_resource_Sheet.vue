<script setup>
//This file is generated by Table2API.
//If you met a bug, please tell me, thanks!
//wrx 20240105
import { onMounted, ref } from 'vue';
import { NForm, NFormItem, NInput, NButton, NSpace } from 'naive-ui';
import { GetOne, Add, Update } from './rfa_resource_Request';
import { translate } from '@utility/i18n.js';
const inPara = defineProps({
  initId: String,
});

const emit = defineEmits(['sheetSubmit']);

const formRef = ref(null); //表格
const sheetData = ref({}); //表单数据
const isNew = ref(true);   //是否是新建

//表单规则
const sheetRules = {
    rfa_code: [
        {
            required: true,
            message: translate.t('族编码') + translate.t('不能为空'),
            trigger: ['input', 'blur'],
        },
    ],
    file_name: [
        {
            required: true,
            message: translate.t('族文件名称') + translate.t('不能为空'),
            trigger: ['input', 'blur'],
        },
    ],
};

onMounted(() => {
    if (inPara.initId != '') {
        isNew.value = false;
        GetOne(inPara.initId).then((res) => {
            sheetData.value = res;
        });
    }
});

//提交
function btnSubmit() {
    formRef.value
        ?.validate((errors) => {
            if (!errors) {
                if (isNew.value) {
                    Add(sheetData.value).then((res) => {
                        emit('sheetSubmit', 'done');
                    });
                } else {
                    Update(sheetData.value).then((res) => {
                        emit('sheetSubmit', 'done');
                    });
                }
            }
        })
        .catch((err) => {
            window.$message.warning(err[0][0].message);
        });
}

//取消
function btnCancel() {
  emit('sheetSubmit', 'cancel');
}

</script>

<template>
  <NForm
    ref="formRef"
        :rules="sheetRules"
    :model="sheetData"
    label-placement="left"
    :label-width="120"
    require-mark-placement="right-hanging"
    size="medium"
    :style="{
      maxWidth: '640px'
    }"
  >
    <div hidden>
        <NFormItem :label="translate.t('主键Id')" path="id">
          <NInput
            :placeholder="translate.t('请输入') + ' ' + translate.t('主键Id')"
            v-model:value="sheetData.id"
          />
        </NFormItem>
    </div>
    <NFormItem :label="translate.t('族编码')" path="rfa_code">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('族编码')"
        v-model:value="sheetData.rfa_code"
      />
    </NFormItem>
    <NFormItem :label="translate.t('族类型名')" path="symbol_name">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('族类型名')"
        v-model:value="sheetData.symbol_name"
      />
    </NFormItem>
    <NFormItem :label="translate.t('族文件地址')" path="rfa_path">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('族文件地址')"
        v-model:value="sheetData.rfa_path"
      />
    </NFormItem>
    <NFormItem :label="translate.t('族文件名称')" path="file_name">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('族文件名称')"
        v-model:value="sheetData.file_name"
      />
    </NFormItem>
    <NFormItem :label="translate.t('族文件大小')" path="file_size">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('族文件大小')"
        v-model:value="sheetData.file_size"
      />
    </NFormItem>
    <NFormItem :label="translate.t('说明')" path="note">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('说明')"
        v-model:value="sheetData.note"
      />
    </NFormItem>
    <NFormItem :label="translate.t('创建账号')" path="create_account">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('创建账号')"
        v-model:value="sheetData.create_account"
      />
    </NFormItem>
    <NFormItem :label="translate.t('创建人')" path="create_username">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('创建人')"
        v-model:value="sheetData.create_username"
      />
    </NFormItem>
    <NFormItem :label="translate.t('创建时间')" path="create_time">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('创建时间')"
        v-model:value="sheetData.create_time"
      />
    </NFormItem>
    <NFormItem :label="translate.t('更新账号')" path="update_account">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('更新账号')"
        v-model:value="sheetData.update_account"
      />
    </NFormItem>
    <NFormItem :label="translate.t('更新人')" path="update_username">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('更新人')"
        v-model:value="sheetData.update_username"
      />
    </NFormItem>
    <NFormItem :label="translate.t('更新时间')" path="update_time">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('更新时间')"
        v-model:value="sheetData.update_time"
      />
    </NFormItem>
    <NFormItem :label="translate.t('备注')" path="remark">
      <NInput
        :placeholder="translate.t('请输入') + ' ' + translate.t('备注')"
        v-model:value="sheetData.remark"
      />
    </NFormItem>
    <NFormItem :label="' '">
      <NSpace>
        <NButton type="primary" @click="btnSubmit">{{ translate.t('提交') }}</NButton>
        <NButton type="primary" @click="btnCancel">{{ translate.t('取消') }}</NButton>
      </NSpace>
    </NFormItem>
  </NForm>
</template>

<style scoped>
</style>

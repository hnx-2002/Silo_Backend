<script setup>
//This file is generated by Table2API.
//If you met a bug, please tell me, thanks!
//wrx jky 20230719
import { onMounted, ref, computed } from 'vue';
import { make_text_NButton, make_h_NSpace } from '@utility/NaiveHComponent';
import { NSpace, NDataTable, NButton } from 'naive-ui';
import { confirmDel } from '@common/message/confirmDel';
import moment from 'moment';
import Layout_UpDown from '@common/layoutIndi/Layout_UpDown.vue';
import { GetAll, Delete } from './dict_silo_Request';
import { translate } from '@utility/i18n.js';

//显示的列
const columns = ref([]);
//表格数据
const tableData = ref([]);
//加载中
const tableLoading = ref(false);

const emit = defineEmits(['newData', 'editData', 'pickRow']);

const inPara = defineProps({});


onMounted(() => {
    resetColumns();
    initData();
});

//初始化数据
function initData() {
    tableLoading.value = true;
    GetAll().then((res) => {
        tableData.value = res;
        tableLoading.value = false;
    });
}

//重置列
function resetColumns() {
    columns.value = [
        {
            title: computed(() => translate.t('序号')),
            align: 'center',
            width: '50px',
            ellipsis: {
                tooltip: true,
            },
            render(row, index) {
                return index + 1;
            }
        },
        {
            title:  computed(() => translate.t('主键Id')),
            key: 'id',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('排序')),
            key: 'sort',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('库型')),
            key: 'silo_type',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('直径')),
            key: 'silo_diameter',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('库型名称')),
            key: 'silo_name',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('库体直径')),
            key: 'diameter_val',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('使用类别')),
            key: 'using_type',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('参考项目')),
            key: 'projects',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('创建账号')),
            key: 'create_account',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('创建人')),
            key: 'create_username',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('创建时间')),
            key: 'create_time',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('更新账号')),
            key: 'update_account',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('更新人')),
            key: 'update_username',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('更新时间')),
            key: 'update_time',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title:  computed(() => translate.t('备注')),
            key: 'remark',
            ellipsis: {
                tooltip: true,
            },
            sorter: 'default',
        },
        {
            title: computed(() => translate.t('操作')),
            width: 160,
            render(row) {
                let opts = [];
                opts.push(make_text_NButton(translate.t('编辑'), () => editRow(row)));
                opts.push(make_text_NButton(translate.t('删除'), () => confirmDel(translate, () => delRow(row))));
                opts.push(make_text_NButton(translate.t('选中'), () => pickRow(row)));
                return make_h_NSpace(() => opts);
            }
        }
    ];
}
//比较方法
function compareAB(a, b) {
    return a > b ? 1 : (a < b ? -1 : 0);
}

//新任务
function newTask() {
    emit('newData', 'showSheet');
}

//编辑
function editRow(row) {
    emit('editData', row.id);
}

//删除
function delRow(row) {
    Delete([row.id]).then((res) => {
        window.$message.info(res);
        initData();
    });
}

//选中行
function pickRow(row) {
    emit('pickRow', row);
}

</script>

<template>
    <Layout_UpDown :UpHeight="'52px'">
        <template #header>
            <NSpace>
                <NButton type="primary" @click="newTask">{{ translate.t('新dict_silo') }}</NButton>
                <!-- 高级查询 -->
            </NSpace>
        </template>
        <template #list>
            <NDataTable
                ref="table"
                bordered
                striped
                :loading="tableLoading"
                :columns="columns"
                :data="tableData"
                :style="{ height: `100%` }"
                flex-height
            ></NDataTable>
        </template>
    </Layout_UpDown>
</template>
<style scoped></style>

// This file is generated by Table2API.
// If you met a bug, please tell me, thanks!
// wrx 20220126

using System;
using System.Collections.Generic;
using System.Data;
using System.IO;
using System.Linq;
using Microsoft.AspNetCore.Mvc;
using Newtonsoft.Json;
using Newtonsoft.Json.Linq;
using System.Threading.Tasks;
using T2ACore;

namespace PTools_PSilo.Controllers;
/// <summary>
/// Template_silo控制器
/// </summary>
[ApiController]
[Route("/PTools_PSilo/[controller]/[action]")]
[ApiExplorerSettings(GroupName = "Admin")]
[TPOK]
public class Template_siloController : ControllerBase
{
    private readonly ITemplate_silo_Business Template_siloBusiness;
    /// <summary>
    /// 构造函数
    /// </summary>
    public Template_siloController(ITemplate_silo_Business inDI)
    {
        Template_siloBusiness = inDI;
    }

    /// <summary>
    /// 根据主键获取单个实体
    /// </summary>
    /// <param name="inId"></param>
    /// <returns></returns>
    [HttpGet("{inId}")]
    public TPResponse<Template_silo_Class> Get([FromRoute] Guid inId)
    {
        var res = Template_siloBusiness.GetOne(inId);
        return TPResponse.New(res);
    }

    #region 单字段查询
    /// <summary>
    /// 根据Silo_name查询
    /// </summary>
    /// <param name="inSearch_Silo_name">查询字段</param>
    /// <returns></returns>
    [HttpGet("{inSearch_Silo_name}")]
    public TPResponse<List<Template_silo_Class>> Search_Silo_name([FromRoute] string inSearch_Silo_name)
    {
        var res = Template_siloBusiness.SearchBy_Silo_name(inSearch_Silo_name);
        return TPResponse.New(res);
    }

    /// <summary>
    /// 根据Symbol_name查询
    /// </summary>
    /// <param name="inSearch_Symbol_name">查询字段</param>
    /// <returns></returns>
    [HttpGet("{inSearch_Symbol_name}")]
    public TPResponse<List<Template_silo_Class>> Search_Symbol_name([FromRoute] string inSearch_Symbol_name)
    {
        var res = Template_siloBusiness.SearchBy_Symbol_name(inSearch_Symbol_name);
        return TPResponse.New(res);
    }

    /// <summary>
    /// 根据Rfa_path查询
    /// </summary>
    /// <param name="inSearch_Rfa_path">查询字段</param>
    /// <returns></returns>
    [HttpGet("{inSearch_Rfa_path}")]
    public TPResponse<List<Template_silo_Class>> Search_Rfa_path([FromRoute] string inSearch_Rfa_path)
    {
        var res = Template_siloBusiness.SearchBy_Rfa_path(inSearch_Rfa_path);
        return TPResponse.New(res);
    }

    /// <summary>
    /// 根据Create_account查询
    /// </summary>
    /// <param name="inSearch_Create_account">查询字段</param>
    /// <returns></returns>
    [HttpGet("{inSearch_Create_account}")]
    public TPResponse<List<Template_silo_Class>> Search_Create_account([FromRoute] string inSearch_Create_account)
    {
        var res = Template_siloBusiness.SearchBy_Create_account(inSearch_Create_account);
        return TPResponse.New(res);
    }

    /// <summary>
    /// 根据Update_account查询
    /// </summary>
    /// <param name="inSearch_Update_account">查询字段</param>
    /// <returns></returns>
    [HttpGet("{inSearch_Update_account}")]
    public TPResponse<List<Template_silo_Class>> Search_Update_account([FromRoute] string inSearch_Update_account)
    {
        var res = Template_siloBusiness.SearchBy_Update_account(inSearch_Update_account);
        return TPResponse.New(res);
    }

    #endregion
    /// <summary>
    /// 分页高级检索
    /// </summary>
    /// <param name="inSearchModel">查询类</param>
    /// <returns></returns>
    [HttpPost]
    public TPResponse<PagedResponse<Template_silo_Class>> MultiPagedSearch(
        [FromBody] Template_silo_paged_Search inSearchModel)
    {
        var (total, datas) = Template_siloBusiness.MultiPagedSearch(inSearchModel);
        var res = PagedResponse.New(total, datas);
        return TPResponse.New(res);
    }

    /// <summary>
    /// 以template_silo生成选项
    /// </summary>
    /// <returns></returns>
    [HttpGet]
    public TPResponse<List<SelectOption<Guid>>> GetOptions()
    {
        var datas = Template_siloBusiness.GetAll().DistinctBy(x => x.Id);
        var res = new List<SelectOption<Guid>>();
        foreach (var data in datas)
        {
            var option = SelectOption<Guid>.New(data.Silo_name, data.Id);
            res.Add(option);
        }
        return TPResponse.New(res);
    }
    /// <summary>
    /// 添加一个实体
    /// </summary>
    /// <param name="inEntity">实体</param>
    /// <returns></returns>
    [HttpPost]
    [DefaultSetAddUserInfo]
    public TPResponse<Res_2Para> Add([FromBody] Template_silo_Class inEntity)
    {
        //添加一些判断，如：if (inEntity.Id == Guid.Empty) inEntity.Id = FunCommon.MakeSortedGuid();
        if (inEntity.Id == Guid.Empty)
        {
            inEntity.Id = FunCommon.MakeSortedGuid();
        }
        var res = Template_siloBusiness.Add(inEntity);
        var msg = res ? "操作成功" : "操作失败";
        return TPResponse.New(Res_2Para.New(res, msg));
    }

    /// <summary>
    /// 修改一个实体
    /// </summary>
    /// <param name="inEntity">实体</param>
    /// <returns></returns>
    [HttpPut]
    [DefaultSetUpdateUserInfo]
    public TPResponse<Res_2Para> Update([FromBody] Template_silo_Class inEntity)
    {
        //添加一些判断，修改操作主键很重要
        var res = Template_siloBusiness.Update(inEntity);
        var msg = res ? "操作成功" : "操作失败";
        return TPResponse.New(Res_2Para.New(res, msg));
    }

    /// <summary>
    /// 删除一批实体
    /// </summary>
    /// <param name="arr">Id集合</param>
    /// <returns></returns>
    [HttpDelete]
    public TPResponse<Res_2Para> Delete([FromBody] Guid[] arr)
    {
        var res = Template_siloBusiness.DeleteByIds(arr);
        var msg = res == arr.Length ? "操作成功" : "操作失败";
        return TPResponse.New(Res_2Para.New(res == arr.Length, msg));
    }


    /// <summary>
    /// 以流的方式获取文件
    /// </summary>
    /// <param name="remaining"></param>
    /// <param name="fileBusiness"></param>
    /// <returns></returns>
    [HttpGet("{*remaining}")]
    public async Task<IActionResult> Download(
        [FromRoute] string remaining,
        [FromServices] IFileMange_Business fileBusiness)
    {
        var filePath = remaining.Replace("%2F", "//");

        var fileInfo = new FileInfo(filePath);
        var (ms, msg) = await fileBusiness.GetObjectStreamOss(
            filePath, Config.OssConfig.BucketName);

        //TODO 校验申请文件人的租户是否与资源所属租户一致

        if (msg == "ok")
        {
            return File(ms, FunOss.GetContentType(fileInfo.Name), fileInfo.Name);
        }
        else
        {
            return new JsonResult(new { msg });
        }
    }
}

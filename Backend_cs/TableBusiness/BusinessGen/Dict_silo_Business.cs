// This file is generated by Table2API.
// If you met a bug, please tell me, thanks!
// wrx cyl 20230922

using System;
using SqlSugar;
using System.Collections.Generic;
using T2ACore;
using System.Linq;

namespace PTools_PSilo;

/// <summary>
/// Dict_silo业务类
/// </summary>
public partial class Dict_silo_Business : IDict_silo_Business
{

    ///////////////////////////////////////////////////////////
    //  以下为生成代码
    ///////////////////////////////////////////////////////////


    #region 生成表

#if DEBUG
    /// <summary>
    /// 创建表
    /// </summary>
    /// <returns></returns>
    public bool CreateTable()
    {
        try
        {
            dbHelper.GetDB("Default").CodeFirst.InitTables<Dict_silo_Class>();
            return true;
        }
        catch (Exception)
        {
            return false;
        }
    }
#endif

    #endregion 生成表

    #region 查询
    /// <summary>
    /// 获取所有实体
    /// </summary>
    /// <returns></returns>
    public List<Dict_silo_Class> GetAll()
    {
        var cacheData = CacheFunc.GetFromCache<List<Dict_silo_Class>>(connId + CacheKey);
        if (cacheData == null || cacheData.Count < 1)
        {
            var newData = dbHelper.QueryList<Dict_silo_Class>(connId);
            CacheFunc.Insert(connId + CacheKey, newData);
            return newData;
        }
        else
        {
            return cacheData;
        }
    }

    /// <summary>
    /// 获取所有实体,不使用缓存
    /// 数据量大时需注意
    /// </summary>
    /// <returns></returns>
    public List<Dict_silo_Class> GetAllWithoutCache()
    {
        return dbHelper.QueryList<Dict_silo_Class>(connId);
    }

    /// <summary>
    /// 获取全部数据数量
    /// </summary>
    /// <returns></returns>
    public int GetAllCount()
    {
        return dbHelper.QueryCount<Dict_silo_Class>(connId, (x) => true);
    }

    /// <summary>
    /// 直接从数据库获取分页全部数据
    /// </summary>
    /// <param name="pageIndex"></param>
    /// <param name="pageSize"></param>
    /// <returns></returns>
    public (int TotalCount, List<Dict_silo_Class> Datas) GetAllPaged(int pageIndex, int pageSize)
    {
        return dbHelper.QueryPageList<Dict_silo_Class>(connId,
            (x) => true, (x) => new
            {
                x.Id
            }, default, pageIndex, pageSize);
    }

    /// <summary>
    /// 根据主键得到一批实体
    /// </summary>
    /// <param name="inSet"></param>
    /// <returns></returns>
    public List<Dict_silo_Class> GetByIds(Guid[] inSet)
    {
        return dbHelper.QueryByIds<Dict_silo_Class, Guid>(connId, inSet);
    }

    /// <summary>
    /// 根据主键从全部实体中获取单个实体
    /// </summary>
    /// <param name="id"></param>
    /// <returns></returns>
    public Dict_silo_Class GetOneFromAll(Guid id)
    {
        return GetAll().Find(p => p.Id == id);
    }

    /// <summary>
    /// 根据主键从全部实体中获取单个实体
    /// </summary>
    /// <param name="id"></param>
    /// <returns></returns>
    public Dict_silo_Class GetOne(Guid id)
    {
        return dbHelper.QueryOne<Dict_silo_Class, Guid>(connId, id);
    }

    /// <summary>
    /// 根据Silo_type查询
    /// </summary>
    /// <param name="inSearch_Silo_type">查询字段</param>
    /// <returns></returns>
    public List<Dict_silo_Class> SearchBy_Silo_type(string inSearch_Silo_type)
    {
        return dbHelper.QueryListByClause<Dict_silo_Class>(connId,
            (x) => x.Silo_type == inSearch_Silo_type,
            (x) => x.Silo_type, default);
    }

    /// <summary>
    /// 根据Silo_type集合查询
    /// </summary>
    /// <param name="inSearch_Silo_types">查询字段</param>
    /// <returns></returns>
    public List<Dict_silo_Class> SearchBy_Silo_types(List<string> inSearch_Silo_types)
    {
        return dbHelper.QueryListByClause<Dict_silo_Class>(connId,
            (x) => inSearch_Silo_types.Contains(x.Silo_type),
            (x) => x.Silo_type, default);
    }

    /// <summary>
    /// 根据Silo_diameter查询
    /// </summary>
    /// <param name="inSearch_Silo_diameter">查询字段</param>
    /// <returns></returns>
    public List<Dict_silo_Class> SearchBy_Silo_diameter(string inSearch_Silo_diameter)
    {
        return dbHelper.QueryListByClause<Dict_silo_Class>(connId,
            (x) => x.Silo_diameter == inSearch_Silo_diameter,
            (x) => x.Silo_diameter, default);
    }

    /// <summary>
    /// 根据Silo_diameter集合查询
    /// </summary>
    /// <param name="inSearch_Silo_diameters">查询字段</param>
    /// <returns></returns>
    public List<Dict_silo_Class> SearchBy_Silo_diameters(List<string> inSearch_Silo_diameters)
    {
        return dbHelper.QueryListByClause<Dict_silo_Class>(connId,
            (x) => inSearch_Silo_diameters.Contains(x.Silo_diameter),
            (x) => x.Silo_diameter, default);
    }

    /// <summary>
    /// 根据Silo_name查询
    /// </summary>
    /// <param name="inSearch_Silo_name">查询字段</param>
    /// <returns></returns>
    public List<Dict_silo_Class> SearchBy_Silo_name(string inSearch_Silo_name)
    {
        return dbHelper.QueryListByClause<Dict_silo_Class>(connId,
            (x) => x.Silo_name == inSearch_Silo_name,
            (x) => x.Silo_name, default);
    }

    /// <summary>
    /// 根据Silo_name集合查询
    /// </summary>
    /// <param name="inSearch_Silo_names">查询字段</param>
    /// <returns></returns>
    public List<Dict_silo_Class> SearchBy_Silo_names(List<string> inSearch_Silo_names)
    {
        return dbHelper.QueryListByClause<Dict_silo_Class>(connId,
            (x) => inSearch_Silo_names.Contains(x.Silo_name),
            (x) => x.Silo_name, default);
    }

    /// <summary>
    /// 根据Using_type查询
    /// </summary>
    /// <param name="inSearch_Using_type">查询字段</param>
    /// <returns></returns>
    public List<Dict_silo_Class> SearchBy_Using_type(string inSearch_Using_type)
    {
        return dbHelper.QueryListByClause<Dict_silo_Class>(connId,
            (x) => x.Using_type == inSearch_Using_type,
            (x) => x.Using_type, default);
    }

    /// <summary>
    /// 根据Using_type集合查询
    /// </summary>
    /// <param name="inSearch_Using_types">查询字段</param>
    /// <returns></returns>
    public List<Dict_silo_Class> SearchBy_Using_types(List<string> inSearch_Using_types)
    {
        return dbHelper.QueryListByClause<Dict_silo_Class>(connId,
            (x) => inSearch_Using_types.Contains(x.Using_type),
            (x) => x.Using_type, default);
    }

    /// <summary>
    /// 根据Create_account查询
    /// </summary>
    /// <param name="inSearch_Create_account">查询字段</param>
    /// <returns></returns>
    public List<Dict_silo_Class> SearchBy_Create_account(string inSearch_Create_account)
    {
        return dbHelper.QueryListByClause<Dict_silo_Class>(connId,
            (x) => x.Create_account == inSearch_Create_account,
            (x) => x.Create_account, default);
    }

    /// <summary>
    /// 根据Create_account集合查询
    /// </summary>
    /// <param name="inSearch_Create_accounts">查询字段</param>
    /// <returns></returns>
    public List<Dict_silo_Class> SearchBy_Create_accounts(List<string> inSearch_Create_accounts)
    {
        return dbHelper.QueryListByClause<Dict_silo_Class>(connId,
            (x) => inSearch_Create_accounts.Contains(x.Create_account),
            (x) => x.Create_account, default);
    }

    /// <summary>
    /// 根据Update_account查询
    /// </summary>
    /// <param name="inSearch_Update_account">查询字段</param>
    /// <returns></returns>
    public List<Dict_silo_Class> SearchBy_Update_account(string inSearch_Update_account)
    {
        return dbHelper.QueryListByClause<Dict_silo_Class>(connId,
            (x) => x.Update_account == inSearch_Update_account,
            (x) => x.Update_account, default);
    }

    /// <summary>
    /// 根据Update_account集合查询
    /// </summary>
    /// <param name="inSearch_Update_accounts">查询字段</param>
    /// <returns></returns>
    public List<Dict_silo_Class> SearchBy_Update_accounts(List<string> inSearch_Update_accounts)
    {
        return dbHelper.QueryListByClause<Dict_silo_Class>(connId,
            (x) => inSearch_Update_accounts.Contains(x.Update_account),
            (x) => x.Update_account, default);
    }

    /// <summary>
    /// 高级检索
    /// </summary>
    /// <param name="inSearchModel">查询类</param>
    /// <returns></returns>
    public List<Dict_silo_Class> MultiSearch(Dict_silo_Search inSearchModel)
    {
        var exp = Expressionable.Create<Dict_silo_Class>();
        exp.AndIF((!string.IsNullOrWhiteSpace(inSearchModel.Silo_type)), x => x.Silo_type.Contains(inSearchModel.Silo_type));
        exp.AndIF((!string.IsNullOrWhiteSpace(inSearchModel.Silo_diameter)), x => x.Silo_diameter.Contains(inSearchModel.Silo_diameter));
        exp.AndIF((!string.IsNullOrWhiteSpace(inSearchModel.Silo_name)), x => x.Silo_name.Contains(inSearchModel.Silo_name));
        exp.AndIF((!string.IsNullOrWhiteSpace(inSearchModel.Using_type)), x => x.Using_type.Contains(inSearchModel.Using_type));
        exp.AndIF((!string.IsNullOrWhiteSpace(inSearchModel.Create_account)), x => x.Create_account.Contains(inSearchModel.Create_account));
        exp.AndIF((!string.IsNullOrWhiteSpace(inSearchModel.Update_account)), x => x.Update_account.Contains(inSearchModel.Update_account));
        return dbHelper.QueryListByClause(connId, exp.ToExpression(), x => x.Id);
    }
    /// <summary>
    /// 高级模糊检索
    /// </summary>
    /// <param name="inSearchModel">查询类</param>
    /// <returns></returns>
    public List<Dict_silo_Class> BlurSearch(Dict_silo_Search inSearchModel)
    {
        var exp = Expressionable.Create<Dict_silo_Class>();
        exp.OrIF((!string.IsNullOrWhiteSpace(inSearchModel.Silo_type)), x => x.Silo_type.Contains(inSearchModel.Silo_type));
        exp.OrIF((!string.IsNullOrWhiteSpace(inSearchModel.Silo_diameter)), x => x.Silo_diameter.Contains(inSearchModel.Silo_diameter));
        exp.OrIF((!string.IsNullOrWhiteSpace(inSearchModel.Silo_name)), x => x.Silo_name.Contains(inSearchModel.Silo_name));
        exp.OrIF((!string.IsNullOrWhiteSpace(inSearchModel.Using_type)), x => x.Using_type.Contains(inSearchModel.Using_type));
        exp.OrIF((!string.IsNullOrWhiteSpace(inSearchModel.Create_account)), x => x.Create_account.Contains(inSearchModel.Create_account));
        exp.OrIF((!string.IsNullOrWhiteSpace(inSearchModel.Update_account)), x => x.Update_account.Contains(inSearchModel.Update_account));
        return dbHelper.QueryListByClause(connId, exp.ToExpression(), x => x.Id);
    }
    /// <summary>
    /// 高级分页检索
    /// </summary>
    /// <param name="inSearchModel">查询类</param>
    /// <returns></returns>
    public (int, List<Dict_silo_Class>) MultiPagedSearch(Dict_silo_paged_Search inSearchModel)
    {
        var exp = Expressionable.Create<Dict_silo_Class>();
        exp.AndIF((!string.IsNullOrWhiteSpace(inSearchModel.Silo_type)), x => x.Silo_type.Contains(inSearchModel.Silo_type));
        exp.AndIF((!string.IsNullOrWhiteSpace(inSearchModel.Silo_diameter)), x => x.Silo_diameter.Contains(inSearchModel.Silo_diameter));
        exp.AndIF((!string.IsNullOrWhiteSpace(inSearchModel.Silo_name)), x => x.Silo_name.Contains(inSearchModel.Silo_name));
        exp.AndIF((!string.IsNullOrWhiteSpace(inSearchModel.Using_type)), x => x.Using_type.Contains(inSearchModel.Using_type));
        exp.AndIF((!string.IsNullOrWhiteSpace(inSearchModel.Create_account)), x => x.Create_account.Contains(inSearchModel.Create_account));
        exp.AndIF((!string.IsNullOrWhiteSpace(inSearchModel.Update_account)), x => x.Update_account.Contains(inSearchModel.Update_account));
        var res = dbHelper.QueryPageList(
            connId, exp.ToExpression(),
            x => x.Id, OrderByType.Desc,
            inSearchModel.Page, inSearchModel.PageSize);

        return res;
    }
    /// <summary>
    /// 高级模糊分页检索
    /// </summary>
    /// <param name="inSearchModel">查询类</param>
    /// <returns></returns>
    public (int, List<Dict_silo_Class>) BlurPagedSearch(Dict_silo_paged_Search inSearchModel)
    {
        var exp = Expressionable.Create<Dict_silo_Class>();
        exp.OrIF((!string.IsNullOrWhiteSpace(inSearchModel.Silo_type)), x => x.Silo_type.Contains(inSearchModel.Silo_type));
        exp.OrIF((!string.IsNullOrWhiteSpace(inSearchModel.Silo_diameter)), x => x.Silo_diameter.Contains(inSearchModel.Silo_diameter));
        exp.OrIF((!string.IsNullOrWhiteSpace(inSearchModel.Silo_name)), x => x.Silo_name.Contains(inSearchModel.Silo_name));
        exp.OrIF((!string.IsNullOrWhiteSpace(inSearchModel.Using_type)), x => x.Using_type.Contains(inSearchModel.Using_type));
        exp.OrIF((!string.IsNullOrWhiteSpace(inSearchModel.Create_account)), x => x.Create_account.Contains(inSearchModel.Create_account));
        exp.OrIF((!string.IsNullOrWhiteSpace(inSearchModel.Update_account)), x => x.Update_account.Contains(inSearchModel.Update_account));
        var res = dbHelper.QueryPageList(
            connId, exp.ToExpression(),
            x => x.Id, OrderByType.Desc,
            inSearchModel.Page, inSearchModel.PageSize);

        return res;
    }
    #endregion 查询

    #region 添加
    /// <summary>
    /// 添加一个实体
    /// </summary>
    /// <param name="inEntity"></param>
    /// <returns></returns>
    public bool Add(Dict_silo_Class inEntity)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.AddOne(connId, inEntity);
        });
    }

    /// <summary>
    /// 添加一批实体
    /// </summary>
    /// <param name="inSet"></param>
    /// <returns></returns>
    public int Add(List<Dict_silo_Class> inSet)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.AddBatch(connId, inSet);
        });
    }
    #endregion 添加


    #region 修改
    /// <summary>
    /// 修改一个实体
    /// </summary>
    /// <param name="inEntity">实体</param>
    /// <returns></returns>
    public bool Update(Dict_silo_Class inEntity)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.UpdateOne(connId, inEntity);
        });
    }

    /// <summary>
    /// 修改一批实体
    /// </summary>
    /// <param name="inSet">实体</param>
    /// <returns></returns>
    public int Update(List<Dict_silo_Class> inSet)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.UpdateBatch(connId, inSet);
        });
    }
    #endregion 修改

    #region 删除
    /// <summary>
    /// 删除一个实体
    /// </summary>
    /// <param name="inEntity">实体</param>
    /// <returns></returns>
    public bool Delete(Dict_silo_Class inEntity)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteOne(connId, inEntity);
        });
    }

    /// <summary>
    /// 删除一批实体
    /// </summary>
    /// <param name="inSet">实体集合</param>
    /// <returns></returns>
    public int Delete(List<Dict_silo_Class> inSet)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteBatch(connId, inSet);
        });
    }

    /// <summary>
    /// 根据主键删除一个实体
    /// </summary>
    /// <param name="inId">主键</param>
    /// <returns></returns>
    public bool DeleteById(Guid inId)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteOneById<Dict_silo_Class, Guid>(connId, inId);
        });
    }

    /// <summary>
    /// 根据主键删除一批实体
    /// </summary>
    /// <param name="inArr">主键集合</param>
    /// <returns></returns>
    public int DeleteByIds(Guid[] inArr)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteBatchByIds<Dict_silo_Class, Guid>(connId, inArr);
        });
    }

    /// <summary>
    /// 根据Silo_type删除
    /// </summary>
    /// <param name="inSearch_Silo_type">查询字段</param>
    /// <returns></returns>
    public int DeleteBy_Silo_type(string inSearch_Silo_type)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteByClause<Dict_silo_Class>(connId,
                (x) => x.Silo_type == inSearch_Silo_type);
        });
    }

    /// <summary>
    /// 根据Silo_type集合删除
    /// </summary>
    /// <param name="inSearch_Silo_types">查询字段集合</param>
    /// <returns></returns>
    public int DeleteBy_Silo_types(List<string> inSearch_Silo_types)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteByClause<Dict_silo_Class>(connId,
                (x) => inSearch_Silo_types.Contains(x.Silo_type));
        });
    }

    /// <summary>
    /// 根据Silo_diameter删除
    /// </summary>
    /// <param name="inSearch_Silo_diameter">查询字段</param>
    /// <returns></returns>
    public int DeleteBy_Silo_diameter(string inSearch_Silo_diameter)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteByClause<Dict_silo_Class>(connId,
                (x) => x.Silo_diameter == inSearch_Silo_diameter);
        });
    }

    /// <summary>
    /// 根据Silo_diameter集合删除
    /// </summary>
    /// <param name="inSearch_Silo_diameters">查询字段集合</param>
    /// <returns></returns>
    public int DeleteBy_Silo_diameters(List<string> inSearch_Silo_diameters)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteByClause<Dict_silo_Class>(connId,
                (x) => inSearch_Silo_diameters.Contains(x.Silo_diameter));
        });
    }

    /// <summary>
    /// 根据Silo_name删除
    /// </summary>
    /// <param name="inSearch_Silo_name">查询字段</param>
    /// <returns></returns>
    public int DeleteBy_Silo_name(string inSearch_Silo_name)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteByClause<Dict_silo_Class>(connId,
                (x) => x.Silo_name == inSearch_Silo_name);
        });
    }

    /// <summary>
    /// 根据Silo_name集合删除
    /// </summary>
    /// <param name="inSearch_Silo_names">查询字段集合</param>
    /// <returns></returns>
    public int DeleteBy_Silo_names(List<string> inSearch_Silo_names)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteByClause<Dict_silo_Class>(connId,
                (x) => inSearch_Silo_names.Contains(x.Silo_name));
        });
    }

    /// <summary>
    /// 根据Using_type删除
    /// </summary>
    /// <param name="inSearch_Using_type">查询字段</param>
    /// <returns></returns>
    public int DeleteBy_Using_type(string inSearch_Using_type)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteByClause<Dict_silo_Class>(connId,
                (x) => x.Using_type == inSearch_Using_type);
        });
    }

    /// <summary>
    /// 根据Using_type集合删除
    /// </summary>
    /// <param name="inSearch_Using_types">查询字段集合</param>
    /// <returns></returns>
    public int DeleteBy_Using_types(List<string> inSearch_Using_types)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteByClause<Dict_silo_Class>(connId,
                (x) => inSearch_Using_types.Contains(x.Using_type));
        });
    }

    /// <summary>
    /// 根据Create_account删除
    /// </summary>
    /// <param name="inSearch_Create_account">查询字段</param>
    /// <returns></returns>
    public int DeleteBy_Create_account(string inSearch_Create_account)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteByClause<Dict_silo_Class>(connId,
                (x) => x.Create_account == inSearch_Create_account);
        });
    }

    /// <summary>
    /// 根据Create_account集合删除
    /// </summary>
    /// <param name="inSearch_Create_accounts">查询字段集合</param>
    /// <returns></returns>
    public int DeleteBy_Create_accounts(List<string> inSearch_Create_accounts)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteByClause<Dict_silo_Class>(connId,
                (x) => inSearch_Create_accounts.Contains(x.Create_account));
        });
    }

    /// <summary>
    /// 根据Update_account删除
    /// </summary>
    /// <param name="inSearch_Update_account">查询字段</param>
    /// <returns></returns>
    public int DeleteBy_Update_account(string inSearch_Update_account)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteByClause<Dict_silo_Class>(connId,
                (x) => x.Update_account == inSearch_Update_account);
        });
    }

    /// <summary>
    /// 根据Update_account集合删除
    /// </summary>
    /// <param name="inSearch_Update_accounts">查询字段集合</param>
    /// <returns></returns>
    public int DeleteBy_Update_accounts(List<string> inSearch_Update_accounts)
    {
        return CacheFunc.WithCacheCleared(connId + CacheKey, () =>
        {
            return dbHelper.DeleteByClause<Dict_silo_Class>(connId,
                (x) => inSearch_Update_accounts.Contains(x.Update_account));
        });
    }

    #endregion 删除
}

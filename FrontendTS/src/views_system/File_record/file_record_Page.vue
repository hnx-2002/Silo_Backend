<script setup lang="ts">
//This file is generated by Table2API.
//If you met a bug, please tell me, thanks!
//wrx 20260121
import { onMounted, ref, useTemplateRef } from 'vue';
import type { VNode } from 'vue';
import { NSpace, NDataTable, NButton, NFormItem, NInput, NPagination, NSelect } from 'naive-ui';
import type { DataTableColumns } from 'naive-ui';
import moment from 'moment';
import {
  make_text_NButton,
  make_h_NSpace,
  h_tag,
  make_download_Abutton,
} from '@utility/NaiveHComponent.ts';
import { executeRes } from '@utility/resultExecute.ts';
import { findLabel } from '@utility/OptionsTool.ts';
import { translate } from '@utility/i18n.ts';
import { GetOne, MultiPagedSearch } from './req_file.ts';
import { confirmDel } from '@common/message/confirmDel.ts';
import Layout_UMD from '@common/layoutIndi/Layout_UMD.vue';
import file_record_Form from './file_record_Form.vue';
import type { File_record_Class, File_record_Search } from './file_record.d.ts';

// const itemName = 'File_record';

//检索条件
const queryInfo = ref<File_record_Search>({
  page: 1,
  pageSize: 15,
  parent_id: '',
  file_name: '',
  file_ext: '',
  bucket_name: '',
  tenant: '',
  create_account: '',
});

const tableLoading = ref(false); //加载中
const columns = ref<DataTableColumns<File_record_Class>>([]); //显示的列
const tableData = ref<File_record_Class[]>([]); //表格数据
const totalCount = ref(0); //数据总数

const file_record_FormRef = useTemplateRef('file_record_FormRef'); //表单控件
const emit = defineEmits([]);
const inPara = defineProps({});

onMounted(() => {
  //注意，加载选项时可能需要异步处理
  //TODO,整理成异步形式
  resetColumns();
  initData();
});

//初始化数据
function initData() {
  tableLoading.value = true;
  if (!queryInfo.value.parent_id) {
    queryInfo.value.parent_id = '00000000-0000-0000-0000-000000000000';
  }
  MultiPagedSearch(queryInfo.value)
    .then((res) => {
      tableData.value = res.datas;
      totalCount.value = res.totalCount;
      tableLoading.value = false;
      queryInfo.value.parent_id = '';
    })
    .catch((err) => console.error(err));
}

//重置列
function resetColumns() {
  columns.value = [
    //{
    //  key: '__index',
    //  title: translate.t('序号'),
    //  align: 'center',
    //  width: '50px',
    //  render(_row: File_record_Class, index: number) {
    //      return index + 1;
    //  },
    //},
    //    {
    //      title: translate.t('主键Id'),
    //      key: 'id',
    //      width: '100px',
    //      ellipsis: {
    //        tooltip: true,
    //      },
    //      sorter: 'default',
    //      render(val: File_record_Class, idx: number) {
    //        return val.id;
    //      },
    //    },
    //    {
    //      title: translate.t('父级Id'),
    //      key: 'parent_id',
    //      width: '100px',
    //      ellipsis: {
    //        tooltip: true,
    //      },
    //      sorter: 'default',
    //      render(val: File_record_Class, idx: number) {
    //        return val.parent_id;
    //      },
    //    },
    {
      title: translate.t('排序'),
      key: 'sort',
      width: '100px',
      ellipsis: {
        tooltip: true,
      },
      sorter: 'default',
      render(val: File_record_Class, idx: number) {
        return val.sort;
      },
    },
    {
      title: translate.t('文件名'),
      key: 'file_name',
      width: '100px',
      ellipsis: {
        tooltip: true,
      },
      sorter: 'default',
      render(val: File_record_Class, idx: number) {
        return val.file_name;
      },
    },
    {
      title: translate.t('扩展名'),
      key: 'file_ext',
      width: '100px',
      ellipsis: {
        tooltip: true,
      },
      sorter: 'default',
      render(val: File_record_Class, idx: number) {
        return val.file_ext;
      },
    },
    // {
    //   title: translate.t('文件完整路径'),
    //   key: 'file_path',
    //   width: '100px',
    //   ellipsis: {
    //     tooltip: true,
    //   },
    //   sorter: 'default',
    //   render(val: File_record_Class, idx: number) {
    //     return val.file_path;
    //   },
    // },
    {
      title: translate.t('桶名称'),
      key: 'bucket_name',
      width: '100px',
      ellipsis: {
        tooltip: true,
      },
      sorter: 'default',
      render(val: File_record_Class, idx: number) {
        return val.bucket_name;
      },
    },
    // {
    //   title: translate.t('MD5编码'),
    //   key: 'md5',
    //   width: '100px',
    //   ellipsis: {
    //     tooltip: true,
    //   },
    //   sorter: 'default',
    //   render(val: File_record_Class, idx: number) {
    //     return val.md5;
    //   },
    // },
    {
      title: translate.t('文件长度'),
      key: 'bytes_length',
      width: '100px',
      ellipsis: {
        tooltip: true,
      },
      sorter: 'default',
      render(val: File_record_Class, idx: number) {
        return val.bytes_length;
      },
    },
    // {
    //   title: translate.t('Referer'),
    //   key: 'referer',
    //   width: '100px',
    //   ellipsis: {
    //     tooltip: true,
    //   },
    //   sorter: 'default',
    //   render(val: File_record_Class, idx: number) {
    //     return val.referer;
    //   },
    // },
    // {
    //   title: translate.t('上传的Ip地址'),
    //   key: 'upload_ip',
    //   width: '100px',
    //   ellipsis: {
    //     tooltip: true,
    //   },
    //   sorter: 'default',
    //   render(val: File_record_Class, idx: number) {
    //     return val.upload_ip;
    //   },
    // },
    //    {
    //      title: translate.t('软删除(10否11是)'),
    //      key: 'is_delete',
    //      width: '100px',
    //      ellipsis: {
    //        tooltip: true,
    //      },
    //      sorter: 'default',
    //      render(val: File_record_Class, idx: number) {
    //        return val.is_delete;
    //      },
    //    },
    {
      title: translate.t('租户'),
      key: 'tenant',
      width: '100px',
      ellipsis: {
        tooltip: true,
      },
      sorter: 'default',
      render(val: File_record_Class, idx: number) {
        return val.tenant;
      },
    },
    //    {
    //      title: translate.t('创建人账号'),
    //      key: 'create_account',
    //      width: '100px',
    //      ellipsis: {
    //        tooltip: true,
    //      },
    //      sorter: 'default',
    //      render(val: File_record_Class, idx: number) {
    //        return val.create_account;
    //      },
    //    },
    {
      title: translate.t('创建人'),
      key: 'create_username',
      width: '100px',
      ellipsis: {
        tooltip: true,
      },
      sorter: 'default',
      render(val: File_record_Class, idx: number) {
        return val.create_username;
      },
    },
    {
      title: translate.t('创建时间'),
      key: 'create_time',
      width: '100px',
      ellipsis: {
        tooltip: true,
      },
      sorter: 'default',
      render(val: File_record_Class, idx: number) {
        return moment(val.create_time).utc(true).format('YYYY-MM-DD HH:mm:ss.SSS');
      },
    },
    //    {
    //      title: translate.t('备注'),
    //      key: 'remark',
    //      width: '100px',
    //      ellipsis: {
    //        tooltip: true,
    //      },
    //      sorter: 'default',
    //      render(val: File_record_Class, idx: number) {
    //        return val.remark;
    //      },
    //    },
    {
      key: '__actions',
      title: translate.t('操作'),
      width: 160,
      render(row: File_record_Class) {
        const opts: VNode[] = [];
        opts.push(make_text_NButton(translate.t('查看'), () => checkRow(row)));
        // opts.push(make_text_NButton(translate.t('编辑'), () => editRow(row)));
        // opts.push(
        //   make_text_NButton(translate.t('删除'), () => confirmDel(translate, () => delRow(row))),
        // );
        //opts.push(make_text_NButton(translate.t('选中'), () => pickRow(row)));
        return make_h_NSpace(opts, 'start', false);
      },
    },
  ];
}

//高级查询
function queryList() {
  queryInfo.value.page = 1;
  queryInfo.value.pageSize = 15;
  initData();
}
//重置查询
function resetQuery() {
  queryInfo.value.page = 1;
  queryInfo.value.pageSize = 15;
  queryInfo.value.parent_id = '';
  queryInfo.value.file_name = '';
  queryInfo.value.file_ext = '';
  queryInfo.value.bucket_name = '';
  queryInfo.value.tenant = '';
  queryInfo.value.create_account = '';
  initData();
}

// //新任务
// function newTask() {
//   file_record_FormRef.value?.open('add', {} as File_record_Class, []);
// }

//查看
function checkRow(row: File_record_Class) {
  GetOne(row.id).then((res) => {
    file_record_FormRef.value?.open('edit', res, []);
  });
}

// //编辑
// function editRow(row: File_record_Class) {
//   GetOne(row.id, itemName).then((res) => {
//     file_record_FormRef.value?.open('edit', res, []);
//   });
// }

// //删除
// function delRow(row: File_record_Class) {
//   Delete([row.id], itemName).then((res) => {
//     executeRes(res, translate, () => {
//       initData();
//     });
//   });
// }
//翻页
function pageChange(page: number) {
  queryInfo.value.page = page;
  initData();
}
//改变页码
function pageSizeChange(pageSize: number) {
  queryInfo.value.page = 1;
  queryInfo.value.pageSize = pageSize;
  initData();
}

////选中行
//function pickRow(row: File_record_Class) {
//    console.log(row.id);
//}
</script>

<template>
  <Layout_UMD>
    <template #header>
      <NSpace justify="space-between">
        <!-- 高级查询 -->
        <NSpace>
          <!-- <NFormItem :label="translate.t('父级Id')" label-placement="left">
              <NInput v-model:value="queryInfo.parent_id" />
          </NFormItem> -->
          <NFormItem :label="translate.t('文件名')" label-placement="left">
            <NInput v-model:value="queryInfo.file_name" />
          </NFormItem>
          <NFormItem :label="translate.t('扩展名')" label-placement="left">
            <NInput v-model:value="queryInfo.file_ext" />
          </NFormItem>
          <NFormItem :label="translate.t('桶名称')" label-placement="left">
            <NInput v-model:value="queryInfo.bucket_name" />
          </NFormItem>
          <NFormItem :label="translate.t('租户')" label-placement="left">
            <NInput v-model:value="queryInfo.tenant" />
          </NFormItem>
          <!-- <NFormItem :label="translate.t('创建人账号')" label-placement="left">
            <NInput v-model:value="queryInfo.create_account" />
          </NFormItem> -->
          <NButton type="primary" @click="queryList">{{ translate.t('查询') }}</NButton>
          <NButton @click="resetQuery">{{ translate.t('重置') }}</NButton>
        </NSpace>
        <!-- <NSpace>
          <NButton type="primary" @click="newTask">
            {{ translate.t('添加') }}
          </NButton>
        </NSpace> -->
      </NSpace>
    </template>
    <template #list>
      <NDataTable
        ref="table"
        bordered
        striped
        :loading="tableLoading"
        :columns="columns"
        :data="tableData"
        :style="{ height: `100%` }"
        flex-height
      ></NDataTable>
    </template>
    <template #footer>
      <NSpace>
        <NPagination
          v-model:page="queryInfo.page"
          v-model:page-size="queryInfo.pageSize"
          :item-count="totalCount"
          show-size-picker
          :page-sizes="[15, 30, 45]"
          :on-update:page="pageChange"
          :on-update:page-size="pageSizeChange"
          :prefix="
            ({ itemCount }: { itemCount: number | undefined }) => {
              return translate.t('共') + ' ' + (itemCount || 0) + ' ' + translate.t('条');
            }
          "
        />
      </NSpace>
    </template>
  </Layout_UMD>
  <file_record_Form ref="file_record_FormRef" @sheetDone="initData()"></file_record_Form>
</template>
<style scoped></style>

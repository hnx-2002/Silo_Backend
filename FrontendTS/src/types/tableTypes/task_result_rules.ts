import { translate } from '@utility/i18n.ts';
import { createNumberRequiredRule, createStringRequiredRule } from '@utility/FormValidator.ts';

export function getRules_task_result() {
  return {
  };
}
